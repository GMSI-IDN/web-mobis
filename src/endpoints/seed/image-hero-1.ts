import type { Media } from '@/payload-types'

export const imageHero1: Omit<Media, 'createdAt' | 'id' | 'updatedAt'> = {
  alt: 'Hero banner Mobis rental mobil Calya dan Sigra untuk driver online',
}
