export default function Head() {
  return (
    <>
      {/* Tambahkan meta tag manual frontend di sini bila tidak tersedia lewat Metadata API Next.js. */}
      {/* Contoh: meta verification vendor, theme-color tambahan, atau meta custom pihak ketiga. */}
      <meta name="theme-color" content="#3BAC1F" />
      <meta name="apple-mobile-web-app-title" content="Mobis" />
    </>
  )
}
